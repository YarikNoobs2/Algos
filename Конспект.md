Основа:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
}
```

CHT:
```cpp
const long long MNOGO = 1e12;
struct CHT {
    struct line {
        long long k, b;
        line(){}
        line(long long a, long long b2) {k = a; b = b2;}
        long long get(long long x){ return k * x + b;}
    };
    vector<line> lines;
    vector<long long> dots;

    long long ceild(long long a, long long b) {
        if (a >= 0) return (a + b - 1) / b;
        return a / b;
    }
    long long cross(line a, line b) {
        long long x = ceild((b.b - a.b), (a.k - b.k));
        return x;
    }
    void add(line a) {
        while (lines.size() && cross(lines.back(), a) <= dots.back()) {
            lines.pop_back();
            dots.pop_back();
        }
        if (lines.empty()) {
            dots.push_back(-MNOGO);
        }
        else {
            dots.push_back(cross(lines.back(), a));
        }
        lines.push_back(a);
    }
    long long get(long long x) {
        long long a = upper_bound(dots.begin(), dots.end(), x) - dots.begin() - 1;
        return lines[a].get(x);
    }
};
```
max:
```cpp
CHT *cht = new CHT();
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    vector<int> a(n), d(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i] >> d[i];
    }
    long long last = 0;
    cht->add({n, 0});
    long long res = 0;
    for (int i = n-1; i >= 0; i--) {
        last = -cht->get(d[i]) + a[i] + 1LL * d[i] * i;
        res = max(res, last);
        cht->add({i, -last});
    }
    cout << res;
}
```
min:
```cpp
CHT *cht = new CHT();
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    vector<int> a(n), b(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    for (int i = 0; i < n; i++) {
        cin >> b[i];
    }
    long long last = 0;
    cht->add({b[0], 0});
    for (int i = 1; i < n; i++) {
        last = cht->get(a[i]);
        cht->add({b[i], last});
    }
    cout << last;
}
```
Li Chao:
```cpp
struct line {
	int k = 0, m = 0;
	line() {}
	line(int k, int m): k(k), m(m) {}
	int get(int x) {
		return k * x + m;
	}
};
line t[4 * MAXN];
void upd(int v, int tl, int tr, line L) {
	if (tl > tr) {
		return;
	}
	int tm = (tl + tr) / 2;
	bool l = L.get(tl) > t[v].get(tl);
	bool mid = L.get(tm) > t[v].get(tm);
	if (mid) {
		swap(L, t[v]);
	}
	if (l != mid) {
		upd(2 * v, tl, tm - 1, L);
	}
	else {
		upd(2 * v + 1, tm + 1, tr, L);
	}
}
int get(int v, int tl, int tr, int x) {
	if (tl > tr) {
		return 0;
	}
	int tm = (tl + tr) / 2;
    if (x == tm) {
        return t[v].get(x);
    }	
    if (x < tm) {
		return max(t[v].get(x), get(2 * v, tl, tm - 1, x));
	}
	else {
		return max(t[v].get(x), get(2 * v + 1, tm + 1, tr, x));
	}
}
```

Кратчайшие пути:
DAG:
```cpp
vector<long long> dag_shortest(int n, vector<vector<pair<int,long long>>>& adj, int start) {
    vector<int> indeg(n,0);
    for(int u=0;u<n;++u) for(auto [v,w]:adj[u]) indeg[v]++;
    queue<int> q; for(int i=0;i<n;++i) if(indeg[i]==0) q.push(i);
    vector<int> topo; topo.reserve(n);
    while(!q.empty()){int u=q.front();q.pop();topo.push_back(u);for(auto [v,w]:adj[u]) if(--indeg[v]==0) q.push(v);}    
    vector<long long> dist(n, 4e18); dist[start]=0;
    for(int u: topo){ if(dist[u]==4e18) continue; for(auto [v,w]:adj[u]) if(dist[v] > dist[u] + w) dist[v]=dist[u]+w; }
    return dist;
}
```
Флойд:
```cpp
void floyd_warshall(vector<vector<long long>>& dist, int n) {
    // dist[i][j] = кратчайшее расстояние от i к j
    // Инициализируем: dist[i][i] = 0, остальные = INF или веса рёбер
    
    for (int k = 0; k < n; k++) {
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (dist[i][k] != LLONG_MAX && dist[k][j] != LLONG_MAX) {
                    dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);
                }
            }
        }
    }
}

void floyd_with_parent(vector<vector<long long>>& dist, vector<vector<int>>& parent, int n){
    for(int k=0;k<n;k++){
        for(int i=0;i<n;i++) if(dist[i][k]!=LLONG_MAX)
            for(int j=0;j<n;j++) if(dist[k][j]!=LLONG_MAX && dist[i][k]+dist[k][j] < dist[i][j]){
                dist[i][j]=dist[i][k]+dist[k][j];
                parent[i][j]=parent[k][j];
            }
    }
}

bool has_negative_cycle_fw(vector<vector<long long>>& dist, int n) {
    floyd_warshall(dist, n);
    for (int i = 0; i < n; i++) {
        if (dist[i][i] < 0) return true;
    }
    return false;
}
```

Форд:
```cpp
vector<long long> spfa(vector<vector<pair<int, long long>>>& adj, int start, int n) {
    vector<long long> dist(n, LLONG_MAX);
    vector<int> cnt(n, 0); // счётчик обновлений
    vector<bool> in_queue(n, false);
    queue<int> q;
    dist[start] = 0;
    q.push(start);
    in_queue[start] = true;
    while (!q.empty()) {
        int u = q.front();
        q.pop();
        in_queue[u] = false;
        for (auto [v, w] : adj[u]) {
            if (dist[u] != LLONG_MAX && dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                cnt[v]++;
                if (cnt[v] > n) {
                    // Отрицательный цикл
                    return vector<long long>(n, LLONG_MIN);
                }
                if (!in_queue[v]) {
                    q.push(v);
                    in_queue[v] = true;
                }
            }
        }
    }
    return dist;
}
```

Дейкстера + Восстановление
```cpp
    while (!q.empty()) {
        auto [d, u] = q.top();
        q.pop();
        if (d > result[u]) continue;
        for (auto [v, w] : g[u]) {
            if (result[v] > result[u] + w) {
                result[v] = result[u] + w;
                path[v] = u;
                q.push({result[v], v});
            }
        }
    }
    if (result[t] == LLONG_MAX) {
        cout << -1;
        return 0;
    }
    cout << result[t] << " ";
    vector<int> res;
    int v = t;
    while (v != -1) {
        res.push_back(v);
        v = path[v];
    }
```

```cpp
    vector<long long> dijkstra(vector<vector<pair<int, long long>>>& adj, int start, int n) {
    vector<long long> dist(n, LLONG_MAX);
    priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<pair<long long, int>>> pq;
    dist[start] = 0;
    pq.push({0, start});
    while (!pq.empty()) {
        auto [d, u] = pq.top();
        pq.pop();
        if (d > dist[u]) continue; // уже обработан с меньшей дистанцией
        for (auto [v, w] : adj[u]) {
            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});
            }
        }
    }
    return dist;
}
```

Мин поток:
```cpp
const int maxn = 305, inf = 1e9;
int n;
int cost[maxn][maxn], cap[maxn][maxn];
int d[maxn], pot[maxn], par[maxn];
bool dijkstra(int s, int t) {
    used[maxn] = {0};
    fill(d, d+n, inf);
    d[s] = 0;
    while (1) {
        int v = -1;
        for (int u = 0; u < n; u++)
            if (!used[u] && (v == -1 && d[u] < d[v]))
                v = u;
        if (v == -1 || d[v] == inf)
            break;
        used[v] = 1;
        for (int u = 0; u < n; u++) {
            int w = cost[v][u] + pot[v] - pot[u];
            if (cap[v][u] && d[u] > d[v] + w) {
                d[u] = d[v] + w;
                par[u] = v;
            }
        }
    }
    return d[t] < inf;
}
int mincost_maxflow(int s, int t) {
    int ans = 0;
    while (dijkstra(s, t)) {
        memcpy(pot, d, sizeof(d));
        int delta = inf;
        for (int v = t; v != s; v = par[v])
            delta = min(delta, cap[par[v]][v]);
        for (int v = t; v != s; v = par[v]) {
            cap[par[v]][v] -= delta;
            cap[v][par[v]] += delta;
            ans += cost[par[v]][v]*delta;
        }
    }
    return ans;
}
```

След перестановка:
```cpp
int perm[MAXN];
bool used[MAXN];
void gen(int cur_index) {
  if (cur_index == n) {
    //получена перестановка
  } else {
    for (int i = 1; i <= n; i++) {
     if (!used[i]) {
        used[i] = 1;
        perm[cur_index] = i;
        used[i] = 0;
     }
  }
}

Или:
next_permutation
```

Евклид:
```cpp
int gcd(int a, int b, int &x, int &y) {
    if (a == 0) {
        x = 0;
        y = 1;
        return b;
    }
    int x1, y1;
    int d = gcd(b % a, a, x1, y1);
    x = y1 - (b / a) * x1;
    y = x1;
    return d;
}
```
Деление:
находим обратный элемент как a^(p-2)
ИЛИ
```cpp
int inv(int a, int m) {
    if (a == 1)
        return 1;
    return (1 - 1ll * inv(m % a, a) * m) / a + m;
}
```

n!/(n-k)!k!
```cpp
int c(int n, int k) {
    return t[n] * inv(t[k] * t[n - k] % mod) % mod;
}
```
Обратный факториал:
```cpp
// обычные факториалы:
int f[maxn];
f[0] = 1;
for (int i = 1; i < maxn; i++)
    f[i] = i * f[i - 1] % mod;
// обратные факториалы:
int r[maxn];
r[maxn - 1] = inv(f[maxn - 1])
for (int i = maxn - 1; i >= 1; i--)
    r[i-1] = r[i] * i % mod;
```
Куча:
```cpp
void sift_up(int v) {
    // если элемент больше своего отца, то всё корректно и больше ничего делать не нужно
    while (v > 1 && t[v] < t[v / 2]) {
        // иначе меняем его с отцом и запускаемся от отца
        swap(t[v], t[v / 2]);
        v /= 2;
    }
}
void sift_down(int v) {
    // пока не пришли в лист
    while (2 * v <= n) { // n -- количество элементов в куче
        int l = 2 * v; // левый сын
        int r = 2 * v + 1; // правый сын
        // если правый сын существует и меньше, выбираем его
        int u = (r <= n && t[r] < t[l] ? r : l);
        if (t[v] <= t[u])
            break; // инвариант и так выполняется, завершаемся
        swap(t[v], t[u]);
        v = u;
   }
}
```
НОП:
```python
func lcs(A: str, B: str) -> int32:
    n = len(A)
    m = len(B)
    
    # dp[i][j] = НОП для A[0..i-1] и B[0..j-1]
    dp = массив размером (n+1) × (m+1) инициализирован нулями
    for i in 1..n:
        for j in 1..m:
            if A[i-1] == B[j-1]:
                dp[i][j] = dp[i-1][j-1] + 1
            else:
                dp[i][j] = max(dp[i-1][j], dp[i][j-1])
    return dp[n][m]
func reconstruct_lcs(A: str, B: str, dp: array) -> str:
    n = len(A)
    m = len(B)
    result = ""
    i = n
    j = m
    while i > 0 and j > 0:
        if A[i-1] == B[j-1]:
            result = A[i-1] + result
            i -= 1
            j -= 1
        elif dp[i-1][j] > dp[i][j-1]:
            i -= 1
        else:
            j -= 1
    return result
result = lcs("ABCD", "ACD")
```
НВП
```
func lis_nlogn_with_builtin(arr: array<int32>) -> int32:
    tail = []
    for x in arr:
        # lower_bound — встроенная функция для двоичного поиска
        # Возвращает первый элемент >= x
        it = lower_bound(tail, x)
        
        if it == end(tail):
            tail.append(x)
        else:
            *it = x  # Разыменовываем и заменяем
    return len(tail)
func reconstruct_lis(arr: array<int32>) -> array<int32>:
    n = len(arr)
    dp = массив размером n инициализирован единицами
    parent = массив размером n инициализирован (-1)
    for i in 1..n-1:
        for j in 0..i-1:
            if arr[j] < arr[i] and dp[j] + 1 > dp[i]:
                dp[i] = dp[j] + 1
                parent[i] = j  # Запоминаем, откуда пришли
    max_length = max_element(dp)
    end_index = index_of(dp, max_length)  # Индекс максимума
    result = []
    current = end_index
    while current != -1:
        result.prepend(arr[current])  # Добавляем в начало
        current = parent[current]
    return result
```
Треугольник:
```cpp
vector<int> g[maxn], p(n);
iota(p.begin(), p.end(), 0);
auto cmp = [&](int a, int b) {
    return g[a].size() < g[b].size() || (g[a].size() == g[b].size() && a < b);
};
sort(p.begin(), p.end(), cmp);
for (int v = 0; v < n; ++v)
    sort(g[v].begin(), g[v].end(), cmp);
vector<int> cnt(n, 0);
int ans = 0;
for (int v : p) {
    for (int w : g[v])
        cnt[w]++;
    for (int u : g[v]) {
        if (cmp(v, u))
            break;
        for (int w : g[u]) {
            if (cmp(u, w))
                break;
            ans += cnt[w];
        }
    }
    for (int w : g[v])
        cnt[w]--;
}
```

Корнячка по запросам:

Рассмотрим стандартную задачу: есть массив размера $n$, и требуется выполнять $q$ запросов прибавления на отрезке и суммы на отрезке. (Про существование [дерева отрезков](/cs/segment-tree) и [прочих деревьев](/cs/range-queries) временно забудем.)

Если бы запросов обновления не было, мы бы решали эту задачу просто массивом [префиксных сумм](/cs/range-queries/prefix-sum):
```c++
int s[maxn]; // [0, r)
s[0] = 0

for (int i = 0; i < n; i++)
    s[i+1] = s[i] + a[i];
```
Вернемся к исходной задаче. Разобьём все запросы изменения на блоки размера примерно $\sqrt q$, и будем честно каждые $\sqrt q$ запросов строить массив префиксных сумм за $O(n)$ с учетом всех предыдущих запросов.
Теперь для ответа на каждый запрос суммы мы можем потратить $O(1)$ времени на запрос к префиксным суммам для предыдущего чекпойнта плюс $O(\sqrt q)$ времени на «корректировку», просто линейно проходясь по всем ещё не учтенным запросам.
```c++
const int c = 300; // ~= sqrt(q)
struct query { int l, r, x; };
vector<query> buffer; // запросы, не учтенные в префиксных суммах
int sum(int l, int r) {
    int res = s[r + 1] - s[l];
    for (query q : buffer)
        // пересечем запрос суммы со всеми запросами
        res += q.x * max(0, min(r, q.r) - max(l, q.l));
    return res;
}
void rebuild() {
    vector<int> d(n, 0); // массив дельт
    for (query q : buffer) {
        d[q.l] += x;
        d[q.r + 1] -= x;
    }
    buffer.clear();

    int delta = 0, running_sum = 0;
    for (int i = 1; i <= n; i++) {
        p[i] += running_sum;
        delta += d[i];
        running_sum += delta;
    }
}
void upd(int l, int r, int x) {
    buffer.push_back({l, r, x});
    if (buffer.size() == c)
        // буфер слишком большой; надо пересчитать префиксные суммы и очистить его
        rebuild();
}
```
Такое решение будет работать за $O(n \sqrt q + q \sqrt q)$: каждые $\sqrt q$ запросов мы перестраиваем префиксные суммы за $O(n)$, и каждый запрос суммы мы просматриваем $O(\sqrt q)$ предыдущих.

ТопСорт:
```cpp
void dfs(int v) {
    used[v] = true;
    for (int u : g[v])
        if (!used[u])
            dfs(u);
    t.push_back(v);
}
void topological_sort() {
    for (int v = 0; v < n; v++)
        if (!used[v])
            dfs(v);
    reverse(t.begin(), t.end());
}
```
Рюкзак:
```python
func knapsack_01_1d(w: array<int32>, v: array<int32>, W: int32) -> int32:
    # dp[cap] = max стоимость с capacity <= cap
    dp = массив размером (W+1) инициализирован нулями
    for i in 0..len(w)-1:
        # ⚠️ ВАЖНО: проходим справа налево!
        for cap in W..w[i]-1 step -1:
            dp[cap] = max(dp[cap], dp[cap - w[i]] + v[i])
    return dp[W]
func reconstruct_01_knapsack(w: array<int32>, v: array<int32>, dp: array, W: int32) -> array<int32>:
    n = len(w)
    selected = []
    cap = W
    for i in n..1 step -1:
        # Если значение пришло из "взяли предмет"
        if cap >= w[i-1] and dp[i][cap] == dp[i-1][cap - w[i-1]] + v[i-1]:
            selected.prepend(i-1)  # Предмет i (0-indexed i-1)
            cap -= w[i-1]
    return selected
func knapsack_unbounded(w: array<int32>, v: array<int32>, W: int32) -> int32:
    # dp[cap] = max стоимость с capacity <= cap
    dp = массив размером (W+1) инициализирован нулями
    for cap in 1..W:
        for i in 0..len(w)-1:
            # Если предмет i влезает
            if w[i] <= cap:
                # Может быть выбран несколько раз
                dp[cap] = max(dp[cap], dp[cap - w[i]] + v[i])
    return dp[W]
func knapsack_counting_01(w: array<int32>, W: int32) -> int64:
    # dp[cap] = кол-во способов получить ровно вес cap
    dp = массив размером (W+1) инициализирован нулями
    dp[0] = 1  # Один способ — ничего не брать
    for i in 0..len(w)-1:
        # Справа налево для 0-1 рюкзака
        for cap in W..w[i]-1 step -1:
            dp[cap] += dp[cap - w[i]]
    return dp[W]
```
Комиваяжёр:
```
func traveling_salesman_bitmask(dist: array<array>) -> int64:
    N = len(dist)
    INF = positive_infinity
    # dp[посещённые города][последний город]
    dp = массив (1 << N) × N инициализирован INF
    dp[1][0] = 0  # Начинаем в городе 0, посетили только его
    for mask in 1..(1 << N):
        for i in 0..N-1:
            if dp[mask][i] == INF:
                continue
            if not (mask & (1 << i)):  # City i not in mask
                continue
            
            # Пробуем идти в город j
            for j in 0..N-1:
                if mask & (1 << j) == 0:  # Город j не посещали
                    new_mask = mask | (1 << j)
                    dp[new_mask][j] = min(dp[new_mask][j], 
                                          dp[mask][i] + dist[i][j])
    # Ищем минимум среди всех завершений (возврат в город 0)
    all_visited = (1 << N) - 1
    result = INF
    for i in 1..N-1:
        result = min(result, dp[all_visited][i] + dist[i][0])
    return result
```
Геома:
```cpp
struct Point {
    long long x, y;

    Point() {};
    Point(long long x, long long y) : x(x), y(y) {};
    double angle() {
        return atan2(y, x);
    }
};
Point operator+(const Point &a, const Point &b) { return {a.x + b.x, a.y + b.y}; }
Point operator-(const Point &a, const Point &b) { return {a.x - b.x, a.y - b.y}; }
long long operator*(const Point &a, const Point &b) { return a.x * b.x + a.y * b.y; }
long long operator^(const Point &a, const Point &b) { return a.x*b.y - b.x*a.y; }
istream& operator>>(istream &in, Point &p) {
    in >> p.x >> p.y;
    return in;
}
ostream& operator<<(ostream &out, Point &p) {
    out << p.x << " " << p.y << endl;
    return out;
}
struct seg {
    Point a, b;
    seg() {};
    seg(Point a, Point b) : a(a), b(b) {
        int r = b.x - a.x;
        int c = a.y - b.y;
    };
    long double gety(int x) {
        if (a.x == b.x) return -1;
        return a.y + 1.0l * (b.y - a.y) * (x - a.x) / (b.x - a.x);
    }
    Point vec() const {
        return b - a;
    }
    long long len2() const {
        Point v = vec();
        return v * v;
    }
    double len() const {
        return sqrt(len2());
    }
    bool contains(const Point &p) const {
        if (((b - a) ^ (p - a)) != 0) return false;
        return min(a.x, b.x) <= p.x && p.x <= max(a.x, b.x) && min(a.y, b.y) <= p.y && p.y <= max(a.y, b.y);
    }
};
long long orient(const Point &a, const Point &b, const Point &c) {
    return (b - a) ^ (c - a);
}
bool intersect(const seg &s1, const seg &s2) {
    Point a = s1.a, b = s1.b;
    Point c = s2.a, d = s2.b;
    long long o1 = orient(a, b, c);
    long long o2 = orient(a, b, d);
    long long o3 = orient(c, d, a);
    long long o4 = orient(c, d, b);
    if (o1 == 0 && s1.contains(c)) return true;
    if (o2 == 0 && s1.contains(d)) return true;
    if (o3 == 0 && s2.contains(a)) return true;
    if (o4 == 0 && s2.contains(b)) return true;
    return (o1 > 0) != (o2 > 0) && (o3 > 0) != (o4 > 0);
}
long double dist(const Point &a, const Point &b) {
    Point v = a - b;
    return sqrtl(v * v);
}
long double distPS(const seg &a, const Point &b) {
    Point v = a.b - a.a;
    Point u = b - a.a;
    long double d = v ^ u;
    return d * d / (v * v);
}
istream& operator>>(istream &in, seg &s) {
    int x1, y1;
    int x2, y2;
    in >> x1 >> y1 >> x2 >> y2;
    if (x1> x2) {
        swap(x1, x2);
        swap(y1, y2);
    }
    if (x1 == x2) {
        if (y1 > y2) swap(y1, y2);
    }
    s = {{x1, y1}, {x2, y2}};
    return in;
}
vector<Point> obol(vector<Point> v) {
    Point f = v[0];
    for (Point i : v) {
        if (i.x < f.x || (i.x == f.x && i.y < f.y)) f = i;
    }
    sort(v.begin(), v.end(), [&](Point &a, Point &b) {
        long long res = (a - f) ^ (b - f);
        if (res != 0) return res > 0;
        Point a1 = a - f;
        Point b1 = b - f;
        return (a1 * a1) < (b1 * b1);
    });
    vector<Point> res;
    for (Point i : v) {
        while (res.size() >= 2) {
            Point n = i - res[res.size() - 1];
            Point l = res[res.size() - 1] - res[res.size() - 2];
            if ((n ^ l) > 0) {
                res.pop_back();
            }
            else break;
        }
        res.push_back(i);
    }
    return res;
}

vector<seg> w;
struct E {
    int i;
    long long x;
    bool isa;
    bool isup;
};
int x = 0;
struct comp {
    using is_transparent = void;
    bool operator()(const int &a, const int &b) const {
        if (fabsl(w[a].gety(x) - w[b].gety(x)) < 1e-18) return a < b;
        return w[a].gety(x) < w[b].gety(x);
    }
    bool operator()(const int &a, long long y) const {
        return w[a].gety(x) < y;
    }
    bool operator()(long long y,const int &a) const {
        return y < w[a].gety(x);
    }
};
const long double EPS = 1e-12;
struct Point2 {
    long double x, y;
};
Point2 operator+(const Point2 &a, const Point2 &b) {
    return {a.x + b.x, a.y + b.y};
}
Point2 operator-(const Point2 &a, const Point2 &b) {
    return {a.x - b.x, a.y - b.y};
}
Point2 operator*(const Point2 &a, const long double &k) {
    return {a.x * k, a.y * k};
}
long double dot(const Point2 &a, const Point2 &b) {
    return a.x * b.x + a.y * b.y;
}
long double cross(const Point2 &a, const Point2 &b) {
    return a.x * b.y - a.y * b.x;
}
Point2 tod(const Point &p) {
    return {1.0l * p.x, 1.0l * p.y};
}
long double dist2(Point2 a, Point2 b) {
    Point2 v = a - b;
    return dot(v, v);
}
ostream& operator<<(ostream &out, Point2 &p) {
    out << p.x << " " << p.y << endl;
    return out;
}
vector<Point> v;
struct mec {
    Point2 c;
    long double r;
    mec() {
        c = {0, 0};
        r = -1;
    }
    mec(Point a) {
        c = tod(a);
        r = 0;
    }
    mec(Point a, Point b) {
        Point2 a2 = tod(a);
        Point2 b2 = tod(b);
        c = (a2 + b2) * 0.5L;
        r = sqrtl(dist2(a2, b2)) * 0.5L;
    }
    mec(Point a, Point b, Point c_) {
        Point2 a2 = tod(a);
        Point2 b2 = tod(b);
        Point2 c2 = tod(c_);
        Point2 ab = b2 - a2;
        Point2 ac = c2 - a2;
        long double d = 2.0L * cross(ab, ac);
        if (fabsl(d) < 1e-17) {
            mec c1(a, b), c2(a, c_), c3(b, c_);
            *this = c1;
            if (c2.r > r) *this = c2;
            if (c3.r > r) *this = c3;
            return;
        }
        long double ab2 = dot(ab, ab);
        long double ac2 = dot(ac, ac);
        c.x = a2.x + (ac.y * ab2 - ab.y * ac2) / d;
        c.y = a2.y + (ab.x * ac2 - ac.x * ab2) / d;
        r = sqrtl(dist2(c, a2));
    }
};
bool inside(mec const& C, Point p) {
    return dist2(C.c, tod(p)) <= (C.r + EPS) * (C.r + EPS);
}
mt19937_64 gen(chrono::steady_clock::now().time_since_epoch().count());
mec enclosing_circle(vector<Point> &p) {
    int n = p.size();
    shuffle(p.begin(), p.end(), gen);
    auto C = mec{p[0], p[1]};
    for(int i = 0; i < n; i++) {
        if(!inside(C, p[i])) {
            C = mec{p[i], p[0]};
            for(int j = 0; j < i; j++) {
                if(!inside(C, p[j])) {
                    C = mec{p[i], p[j]};
                    for(int k = 0; k < j; k++) {
                        if(!inside(C, p[k])) {
                            C = mec{p[i], p[j], p[k]};
                        }
                    }
                }
            }
        }
    }
    return C;
}
int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    int n;
    cin >> n;
    vector<E> v;
    w.resize(n);
    for (int i = 0; i < n; i++) {
        cin >> w[i];
        v.push_back({i, w[i].a.x, true, w[i].a.x == w[i].b.x});
        if (w[i].a.x != w[i].b.x)v.push_back({i, w[i].b.x, false, w[i].a.x == w[i].b.x});
    }
    sort(v.begin(), v.end(), [&](const E &a, const E &b) {
        if (a.x != b.x) return a.x < b.x;
        return a.isup < b.isup;
    });
    set<int, comp> s;
    for (auto i : v) {
        x = i.x;
        if (i.isup) {
            auto j = s.upper_bound(w[i.i].b.y * 1LL);
            if (j == s.begin()) continue;
            j = prev(j);
            if (w[*j].gety(x) >= w[i.i].a.y) {
                cout << "YES";
                return 0;
            }
            continue;
        }
        if (i.isa) {
            auto j = s.insert(i.i).first;
            if (j != s.begin()) {
                auto l = prev(j);
                if (intersect(w[*j], w[*l])) {
                    cout << "YES";
                    return 0;
                }
            }
            auto r = next(j);
            if (r != s.end()) {
                if (intersect(w[*j], w[*r])) {
                    cout << "YES";
                    return 0;
                }
            }
        }
        else {
            auto j = s.find(i.i);
            auto l = j == s.begin() ? s.end() : prev(j);
            auto r = next(j);
            if (l != s.end() && r != s.end()) {
                if (intersect(w[*l], w[*r])) {
                    cout << "YES";
                    return 0;
                }
            }
            s.erase(j);
        }
    }
    cout << "NO";
    return 0;
}
```
Тернарник:
```cpp
double l = ..., r = ..., EPS = ...; // входные данные
while (r - l > EPS) {
   double m1 = l + (r - l) / 3,
      m2 = r - (r - l) / 3;
   if (f (m1) < f (m2))
      l = m1;
   else
      r = m2;
}
```
sos-dp:
```
func sos_zeta(F: array<int64>, n: int32):
    # На входе: F = f (исходные значения)
    # На выходе: F[mask] = Σ_{sub ⊆ mask} f[sub]
    for bit in 0..n-1:
        for mask in 0..(1<<n)-1:
            if (mask & (1<<bit)) != 0:  # Если бит установлен
                # Добавляем вклад маски без этого бита
                F[mask] += F[mask ^ (1<<bit)]
                #        ↑ XOR сбросит бит, дав нам маску-без-бита
```
Прим nm:
```cpp
const int maxn = 1e5, inf = 1e9;
vector from, to, weight;
bool used[maxn]
used[0] = 1;
for (int i = 0; i < n-1; i++) {
    int opt_w = inf, opt_from, opt_to;
    for (int j = 0; j < m; j++)
        if (opt_w > weight[j] && used[from[j]] && !used[to[j]])
            opt_w = weight[j], opt_from = from[j], opt_to = to[j]
    used[opt_to] = 1;
    cout << opt_from << " " << opt_to << endl;
}
```
Прим n^2
```cpp
const int maxn = 1e5, inf = 1e9;
bool used[maxn];
vector< pair<int, int> > g[maxn];
int min_edge[maxn] = {inf}, best_edge[maxn];
min_edge[0] = 0;
for (int i = 0; i < n; i++) {
    int v = -1;
    for (int u = 0; u < n; u++)
        if (!used[u] && (v == -1 || min_edge[u] < min_edge[v]))
            v = u;
    used[v] = 1;
    if (v != 0)
        cout << v << " " << best_edge[v] << endl;
    for (auto e : g[v]) {
        int u = e.first, w = e.second;
        if (w < min_edge[u]) {
            min_edge[u] = w;
            best_edge[u] = v;
        }
    }
}
```
mlogn:
```cpp
set< pair<int, int> > q;
int d[maxn];
while (q.size()) {
    v = q.begin()->second;
    q.erase(q.begin());
    for (auto e : g[v]) {
        int u = e.first, w = e.second;
        if (w < d[u]) {
            q.erase({d[u], u});
            d[u] = w;
            q.insert({d[u], u});
        }
    }
}
```
Сочленение:
```cpp
void dfs(int v, int p = -1) {
    used[v] = 1;
    d[v] = h[v] = (p == -1 ? 0 : h[p] + 1);
    int children = 0;
    for (int u : g[v]) {
        if (u != p) {
            if (used[u])
                d[v] = min(d[v], h[u]);
            else {
                dfs(u, v);
                d[v] = min(d[v], d[u]);
                if (h[v] <= d[u] && p != -1) { // корень (p == -1) обрабатываем отдельно
                    // v -- точка сочленения
                    // (это условие может выполниться много раз для разных детей)
                }
              	children++;
            }
        }
    }
    if (p == -1 && children > 1) {
        // v -- корень и точка сочленения
    }
}
```
Мост:
```cpp
void dfs(int v, int p = -1) {
    used[v] = true;
    d[v] = h[v] = (p == -1 ? 0 : h[p] + 1);
    for (int u : g[v]) {
        if (u != p) {
            if (used[u]) // если ребро обратное
                d[v] = min(d[v], h[u]);
            else { // если ребро прямое
                dfs(u, v);
                d[v] = min(d[v], d[u]);
                if (h[v] < d[u]) {
                    // если нельзя другим путем добраться в v или выше,
                    // то ребро (v, u) -- мост
                }
            }
        }
    }
}
```
z-функция:
```cpp
vector<int> z_function(string s) {
    int n = (int) s.size();
    vector<int> z(n, 0);
    int l = 0, r = 0;
    for (int i = 1; i < n; i++) {
        if (i <= r)
            z[i] = min(r - i + 1, z[i - l]);
        while (i + z[i] < n && s[z[i]] == s[i + z[i]])
            z[i]++;
        if (i + z[i] - 1 > r) {
            r = i + z[i] - 1;
            l = i;
        }
    }
    return z;
}
```
Префикс можно онлайн:
```cpp
vector<int> prefix_function(string s) {
    int n = (int) s.size();
    vector<int> p(n, 0);
    for (int i = 1; i < n; i++) {
        int cur = p[i - 1];
        while (s[i] != s[cur] && cur > 0)
            cur = p[cur - 1];
        if (s[i] == s[cur])
            p[i] = cur + 1;
    }
    return p;
}
```
Манакер:
```cpp
vector<int> manacher_odd(string s) {
    int n = (int) s.size();
    vector<int> d(n, 1);
    int l = 0, r = 0;
    for (int i = 1; i < n; i++) {
        if (i < r)
            d[i] = min(r - i + 1, d[l + r - i]);
        while (i - d[i] >= 0 && i + d[i] < n && s[i - d[i]] == s[i + d[i]])
            d[i]++;
        if (i + d[i] - 1 > r)
            l = i - d[i] + 1, r = i + d[i] - 1;
    }
    return d;
}
```
Фенвик:
```cpp
int sum (int r) {
    int res = 0;
    for (; r > 0; r -= r & -r)
        res += t[r];
    return res;
}
int sum (int l, int r) {
    return sum(r) - sum(l-1);
}
// обновляет нужные t
void add (int k, int x) {
    for (; k <= n; k += k & -k)
        t[k] += x;
}
int sum (int r1, int r2) { //2D
    int res = 0;
    for (int i = r1; i > 0; i -= i & -i)
        for (int j = r2; j > 0; j -= j & -j)
            ans += t[i][j];
    return res;
}
```
DCP offline:
```cpp
vector<int> dsu;
vector<pair<int, int>> st;
int res = 0;
int p(int i) {
    return dsu[i] < 0 ? i : p(dsu[i]);
}
void merge(int a, int b) {
    a = p(a);
    b = p(b);
    if (a == b) {
        st.push_back({-1, -1});
        return;
    }
    if (dsu[a] > dsu[b]) swap(a, b);
    st.push_back({b, dsu[b]});
    dsu[a] += dsu[b];
    dsu[b] = a;
    res--;
}
void undo() {
    if (st.size() == 0) return;
    auto i = st.back();
    st.pop_back();
    if (i.first == -1) return;
    dsu[dsu[i.first]] -= i.second;
    dsu[i.first] = i.second;
    res++;
}
struct Node {
    bool get = false;
    vector<pair<int, int>> v;
};
vector<Node> tree;
void add(int i, int l, int r, int ql, int qr, pair<int, int> x) {
    if (qr < l || r < ql) return;
    if (ql <= l && r <= qr) {
        tree[i].v.push_back(x);
        return;
    }
    int m = (l + r) / 2;
    add(i * 2, l, m, ql, qr, x);
    add(i * 2 + 1, m + 1, r, ql, qr, x);
}
void add(int i, int l, int r, int p) {
    if (l == r) {
        tree[i].get = true;
        return;
    }
    int m = (l + r) / 2;
    if (m >= p) {
        add(i * 2, l, m, p);
    }
    else {
        add(i * 2 + 1, m + 1, r, p);
    }
}
void get(int i, int l, int r) {
    for (auto j : tree[i].v) {
        merge(j.first, j.second);
    }
    if (tree[i].get) {
        cout << res << '\n';
    }
    if (l == r) {
        for (auto j : tree[i].v) {
            undo();
        }
        return;
    }
    int m = (l + r) / 2;
    get(i * 2, l, m);
    get(i * 2 + 1, m + 1, r);
    for (auto j : tree[i].v) {
        undo();
    }
}
```
Ахо:
```cpp
struct Node {
    map<char, Node*> to;
    map<char, Node*> go;
    Node* link = 0;
    Node* p;
    Node* up = 0;
    int d;
    int temp = 0;
    int pch;
    Node(char ch, Node* _p, int d): p(_p), pch(ch), d(d) {}
};
Node* root = new Node(-1, 0, 0);
void add(string &s, int type) {
    Node* v = root;
    for (char i : s) {
        //i = i - 'a';
        if (!v->to[i]) {
            v->to[i] = new Node(i, v, v->d + 1);
        }
        v = v->to[i];
    }
    v->temp = type;
}
Node* go(Node* v, int t);
Node* link(Node* v) {
    if (!v->link) {
        if (v == root || v->p == root) {
            v->link = root;
        }
        else {
            v->link = go(link(v->p), v->pch);
        }
    }
    return v->link;
}
Node* go(Node* v, int t) {
    if (!v->go[t]) {
        if (v->to[t]) {
            v->go[t] = v->to[t];
        }
        else if (v == root) {
            v->go[t] = root;
        }
        else {
            v->go[t] = go(link(v), t);
        }
    }
    return v->go[t];
}
Node* up(Node* v) {
    if (!v->up) {
        Node* u = link(v);
        if (u->temp) {
            v->up = u;
        }
        else if (u == root) {
            v->up = 0;
        }
        else {
            v->up = up(u);
        }
    }
    return v->up;
}
int get_max(Node* v) {
    int mx = 0;
    while (v) {
        if (v->temp) mx = max(mx, v->d);
        v = up(v);
    }
    return mx;
}
vector<int> get_up(Node* v) {
    vector<int> res;
    while (v) {
        if (v->temp) res.push_back(v->temp);
        v = up(v);
    }
    return res;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        string s;
        cin >> s;
        add(s, i+1);
    }
    int m;
    cin >> m;
    cin.ignore();
    for (int i = 0; i < m; i++) {
        string s;
        getline(cin, s);
        Node* v = root;
        for (int j = 0; j < s.size(); j++) {
            v = go(v, s[j]);
            int d = get_max(v);
            for (int k = j - d + 1; k <= j; k++) {
                s[k] = '*';
            }
        }
        cout << s << '\n';
    }
    return 0;
}
```
СуффМасс:
```cpp
vector<int> build(string s) {
    s = s + char(0);
    vector<int> c(s.size());
    vector<int> p(s.size());
    iota(c.begin(), c.end(), 0);
    sort(c.begin(), c.end(), [&s](int a, int b) {
        return s[a] < s[b];
    });
    int cnt = -1;
    for (int i = 0; i < c.size(); i++) {
        if (i == 0 || s[c[i]] != s[c[i-1]]) {
            cnt++;
        }
        p[c[i]] = cnt;
    }
    cnt++;
    for (int i = 1; i <= s.size(); i *= 2) {
        vector<vector<int>> v(cnt);
        for (int l = 0; l < s.size(); l++) {
            int ps = (c[l] - i + s.size()) % s.size();
            v[p[ps]].push_back(ps);
        }
        vector<int> p2(s.size());
        int cnt2 = -1;
        int cnt3 = 0;
        for (int l = 0; l < v.size(); l++) {
            for (int j = 0; j < v[l].size(); j++) {
                if (j == 0 || p[v[l][j]] != p[v[l][j-1]] || p[(v[l][j] + i) % s.size()] != p[(v[l][j-1] + i) % s.size()]) cnt2++;
                p2[v[l][j]] = cnt2;
                c[cnt3++] = v[l][j];
            }
        }
        swap(p, p2);
        cnt = cnt2 + 1;
    }
    return vector<int>(c.begin()+1, c.end());
}
vector<int> lcp(string s, vector<int> c, vector<int> ps) {
    vector<int> res(s.size(), 0);
    int p = 0;
    for (int i = 0; i < s.size(); i++) {
        if (ps[i] == s.size() - 1) continue;
        int pi = c[ps[i] + 1];
        int mi = max(i, pi);
        while (mi + p < s.size() && s[i + p] == s[pi + p]) p++;
        res[i] = p;
        p = max(0, p-1);
    }
    return res;
}
```
Парсочи:
```cpp
bool dfs(int v) {
    if (used[v])
        return false;
    used[v] = true;
    for (int u : g[v]) {
        if (mt[u] == -1 || dfs(mt[u])) {
            mt[u] = v;
            return true;
        }
    }
    return false;
}
```
ДО с откатами:
```cpp
struct SegmentTree {
    SegmentTree* l = 0;
    SegmentTree* r = 0;
    int lb, rb;
    int sm = 0;
    SegmentTree(int lb, int rb): lb(lb), rb(rb) {
        if (lb + 1 > rb) return;
        int m = (lb + rb) / 2;
        l = new SegmentTree(lb, m);
        r = new SegmentTree(m + 1, rb);
        sm = l->sm + r->sm;
    }
    void copy() {
        if (!l) return;
        l = new SegmentTree(*l);
        r = new SegmentTree(*r);
    }
    void add(int k, int x) {
        copy();
        sm += x;
        if (l) {
            if (k > l->rb) {
                r->add(k, x);
            }
            else {
                l->add(k, x);
            }
        }
    }
    int get(int lq, int rq) {
        if (lb > rq || rb < lq) return 0;
        if (lb >= lq && rb <= rq) return sm;
        return l->get(lq, rq) + r->get(lq, rq);
    }
};
vector<SegmentTree*> roots;
void add(int k, int x, int v) {
    if (v >= roots.size()) {
        roots.push_back(new SegmentTree(*roots[v-1]));
    }
    roots[v]->add(k, x);

}
int get(int x, int l, int r) {
    if (x < 0) return 0;
    return roots[x]->get(l, r);
}
int get(int x1, int y1, int x2, int y2) {
    return get(x2, y1, y2) - get(x1-1, y1, y2);
}
```
Центроиды:
```cpp
struct Node {
    unordered_map<int, int> c;
};
vector<bool> chk;
vector<int> sz;
vector<Node> centroids;
vector<vector<int>> g;
vector<int> v;
vector<vector<pair<int, int>>> dist;
void sizes(int i, int p) {
    sz[i] = 1;
    for (auto j : g[i]) {
        if (j != p && !chk[j]) {
            sizes(j, i);
            sz[i] += sz[j];
        }
    }
}
int centroid(int i, int p, int n) {
    for (int j : g[i]) {
        if (j != p && !chk[j] && sz[j] > n/2) {
            return centroid(j, i, n);
        }
    }
    return i;
}
void dfs(int i, int p, int d, int c) {
    dist[i].push_back({c, d});
    if (centroids[c].c.find(v[i]) == centroids[c].c.end()) {
        centroids[c].c[v[i]] = d;
    }
    if (centroids[c].c[v[i]] > d) {
        centroids[c].c[v[i]] = d;
    }
    for (auto j : g[i]) {
        if (j == p || chk[j]) continue;
        dfs(j, i, d+1, c);
    }
}
void solve(int i) {
    sizes(i, -1);
    int c = centroid(i, -1, sz[i]);
    dfs(c, -1, 0, c);
    chk[c] = true;
    for (auto j : g[c]) {
        if (!chk[j]) {
            solve(j);
        }
    }
}
int get(int i, int c) {
    int res = INT_MAX;
    for (auto j : dist[i]) {
        if (centroids[j.first].c.find(c) != centroids[j.first].c.end()) {
            res = min(res, centroids[j.first].c[c] + j.second);
        }
    }
    if (res == INT_MAX) return -1;
    return res;
}
```
HLD:
```cpp
struct gc {
    vector<long long> i;
    long long s;
};
struct Node {
    long long psum = 0;
    long long sum = 0;
    long long mod = 0;
};
struct Res {
    long long sum = 0;
    long long mod = 0;
};
vector<long long> tin, tout, p, ds, s, h;
vector<Node> st;
vector<gc> g;
long long t = 0;
long long norm(long long x) {
    x %= MOD;
    if (x < 0) x += MOD;
    return x;
}
void apply(long long i, long long l, long long r, long long x) {
    long long len = r - l + 1;
    st[i].sum = min(MOD, st[i].sum + x * len);
    st[i].mod = (st[i].mod + norm(x) * len) % MOD;
    st[i].psum += x;
}
void push(long long i, long long l, long long r) {
    if (st[i].psum == 0 || l == r) return;
    long long m = (l + r) / 2;
    apply(i * 2, l, m, st[i].psum);
    apply(i * 2 + 1, m + 1, r, st[i].psum);
    st[i].psum = 0;
}
void pull(long long i) {
    st[i].sum = min(MOD, st[i * 2].sum + st[i * 2 + 1].sum);
    st[i].mod = (st[i * 2].mod + st[i * 2 + 1].mod) % MOD;
}
void add(long long i, long long l, long long r, long long ql, long long qr, long long x) {
    if (ql > r || l > qr || ql > qr) return;
    if (ql <= l && r <= qr) {
        apply(i, l, r, x);
        return;
    }
    push(i, l, r);
    long long m = (l + r) / 2;
    add(i * 2, l, m, ql, qr, x);
    add(i * 2 + 1, m + 1, r, ql, qr, x);
    pull(i);
}
Res get(long long i, long long l, long long r, long long ql, long long qr) {
    if (ql > r || l > qr || ql > qr) {
        return {0, 0};
    }
    if (ql <= l && r <= qr) {
        return {st[i].sum, st[i].mod};
    }
    push(i, l, r);
    long long m = (l + r) / 2;
    auto a = get(i * 2, l, m, ql, qr);
    auto b = get(i * 2 + 1, m + 1, r, ql, qr);
    return {min(MOD, a.sum + b.sum), (a.mod + b.mod) % MOD};
}
bool ancestor(long long a, long long b) {
    return tin[a] <= tin[b] && tin[b] < tout[a];
}
void sizes(long long i = 0) {
    s[i] = 1;
    for (long long &j : g[i].i) {
        sizes(j);
        s[i] += s[j];
        if (s[j] > s[g[i].i[0]]) {
            swap(j, g[i].i[0]);
        }
    }
}
void hld(long long i = 0) {
    tin[i] = t++;
    for (long long j : g[i].i) {
        p[j] = i;
        ds[j] = ds[i] + 1;
        h[j] = (j == g[i].i[0] ? h[i] : j);
        hld(j);
    }
    tout[i] = t;
}
void upd(long long i, long long x) {
    add(1, 0, n - 1, tin[i], tin[i], x);
}
void upadd(long long &a, long long &b, long long x) {
    while (h[a] != h[b]) {
        if (ds[h[a]] < ds[h[b]]) swap(a, b);

        add(1, 0, n - 1, tin[h[a]], tin[a], x);

        a = p[h[a]];
    }
}
void add_path(long long a, long long b, long long x) {
    upadd(a, b, x);

    if (ds[a] > ds[b]) swap(a, b);

    add(1, 0, n - 1, tin[a], tin[b], x);
}
void upget(long long &a, long long &b, Res &res) {
    while (h[a] != h[b]) {
        if (ds[h[a]] < ds[h[b]]) swap(a, b);

        auto d = get(1, 0, n - 1, tin[h[a]], tin[a]);
        res = {min(MOD, res.sum + d.sum), (res.mod + d.mod) % MOD};

        a = p[h[a]];
    }
}
Res get_path(long long a, long long b) {
    Res res{0, 0};
    upget(a, b, res);
    if (ds[a] > ds[b]) swap(a, b);
    auto d = get(1, 0, n - 1, tin[a], tin[b]);
    res = {min(MOD, res.sum + d.sum), (res.mod + d.mod) % MOD};
    return res;
}
long long gets(long long i, long long b = 0) {
    Res s1 = get(1, 0, n - 1, tin[i], tout[i] - 1);
    Res s2 = get_path(i, b);
    Res s3 = get(1, 0, n - 1, tin[i], tin[i]);
    long long ans = (s1.mod + s2.mod - s3.mod) % MOD;
    if (ans < 0) ans += MOD;
    if (s1.sum == MOD || s2.sum == MOD) {
        return ans;
    }
    long long res = s1.sum + s2.sum - s3.sum;
    if (res >= MOD) {
        return res % MOD;
    }
    return res;
}
void madd(long long i, long long x) {
    add_path(0, i, x);
    add(1, 0, n - 1, tin[i], tout[i] - 1, x);
    upd(i, -x);
}
```
Матрицы:
```cpp
int p = 1E+9 + 7;
vector<vector<long long>> mul(vector<vector<long long>> &a, vector<vector<long long>> &b) {
    vector<vector<long long>> c(a.size(), vector<long long>(b[0].size(), 0));
    for (int i = 0; i < a.size(); i++) {
        for (int j = 0; j < b[0].size(); j++) {
            for (int k = 0; k < a[0].size(); k++) {
                c[i][j] += a[i][k] * b[k][j];
                c[i][j] %= p;
            }
        }
    }
    return c;
}
vector<vector<long long>> z(int n) {
    vector<vector<long long>> zs(n, vector<long long>(n, 0));
    for (int i = 0; i < n; i++) zs[i][i] = 1;
    return zs;
}
vector<vector<long long>> bin(vector<vector<long long>> &a, long long n) {
    if (n == 0) return z(a.size());
    if (n == 1) return a;
    auto c = bin(a, n/2);
    c = mul(c, c);
    if (n % 2 == 1) {
        c = mul(c, a);
    }
    return c;
}
int main() {
    long long n;
    cin >> n;
    vector<vector<long long>> c = {{1, 1, 0}, {1, 0, 1}, {1, 0, 0}};
    c = bin(c, n-1);
    vector<vector<long long>> g = {{1, 1, 0}};
    auto res = mul(g, c);
    cout << (res[0][0] + res[0][1] + res[0][2]) % p;
}
//Обращение:
vector<vector<long long>> z(int n) {
    vector<vector<long long>> zs(n, vector<long long>(n, 0));
    for (int i = 0; i < n; i++) zs[i][i] = 1;
    return zs;
}
void gauss(vector<vector<long double>> &a) {
    int n = a.size();
    vector<int> where(n, -1);
    for (int i = 0; i < n; i++) {
        int mx = i;
        for (int j = i + 1; j < n; j++) {
            if (fabsl(a[j][i]) > fabsl(a[mx][i])) {
                mx = j;
            }
        }
        swap(a[i], a[mx]);
        if (fabsl(a[i][i]) < EPS) continue;
        where[i] = i;
        for (int j = 2 * n - 1; j >= i; j--) {
            a[i][j] /= a[i][i];
        }
        for (int j = 0; j < n; j++) {
            if (j == i) continue;
            for (int k = 2 * n - 1; k >= i; k--) {
                a[j][k] -= a[i][k] * a[j][i];
            }
        }
    }
    for (int i = 0; i < n; i++) {
        if (where[i] == -1) {
            cout << "NO\n";
            return;
        }
    }
    cout << "YES\n";
    for (int i = 0; i < n; i++) {
        for (int j = n; j < 2*n; j++)
            cout << fixed << setprecision(10) <<  a[i][j] << ' ';
        cout << '\n';
    }
}
int main() {
    long long n;
    cin >> n;
    vector<vector<long double>> cs(n, vector<long double>(2 * n, 0));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cin >> cs[i][j];
        }
        cs[i][i + n] = 1;
    }
    gauss(cs);
    return 0;
}
```
Гранди + xor на разрезание:
```cpp
int n, m, s;
cin >> n >> m >> s;
vector<vector<int>> g(n, vector<int>(m, -1));
for (int i = 0; i < s; i++) {
    if (s <= m) g[0][i] = 0;
    if (s <= n) g[i][0] = 0;
}
for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
        if (g[i][j] != -1) continue;
        vector<bool> used(max(i, j) + 1, false);
        for (int k = 0; k < i; k++) {
            int x = g[k][j] ^ g[i-k-1][j];
            if (x < used.size()) used[x] = true;
        }
        for (int k = 0; k < j; k++) {
            int x = g[i][k] ^ g[i][j-k-1];
            if (x < used.size()) used[x] = true;
        }
        for (int k = 0; k < used.size(); k++) {
            if (!used[k]) {
                g[i][j] = k;
                break;
            }
        }
    }
}
cout << (g[n-1][m-1] == 0) + 1;
```
FFT:
```cpp
const int MOD = 998244353, W = 805775211, IW = 46809892;
const int MAXN = (1 << 19), INV2 = 499122177;
int pws[MAXN + 1], ipws[MAXN + 1];
void init() {
    pws[MAXN] = W; ipws[MAXN] = IW;
    for (int i = MAXN / 2; i >= 1; i /= 2) {
        pws[i] = (pws[i * 2] * 1ll * pws[i * 2]) % MOD;
        ipws[i] = (ipws[i * 2] * 1ll * ipws[i * 2]) % MOD;
    }
}
void fft(vector<int> &a, vector<int> &ans, int l, int cl, int step, int n, bool inv) {
    if (n == 1) { ans[l] = a[cl]; return; }
    fft(a, ans, l, cl, step * 2, n / 2, inv);
    fft(a, ans, l + n / 2, cl + step, step * 2, n / 2, inv);
    int cw = 1, gw = (inv ? ipws[n] : pws[n]);
    for (int i = l; i < l + n / 2; i++) {
        int u = ans[i], v = (cw * 1ll * ans[i + n / 2]) % MOD;
        ans[i] = (u + v) % MOD;
        ans[i + n / 2] = (u - v) % MOD;
        if (ans[i + n / 2] < 0) ans[i + n / 2] += MOD;
        if (inv) {
            ans[i] = (ans[i] * 1ll * INV2) % MOD;
            ans[i + n / 2] = (ans[i + n / 2] * 1ll * INV2) % MOD;
        }
        cw = (cw * 1ll * gw) % MOD;
    }
}
```
